<%- model_class = Message -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:psz_sub_port) %></th>
      <th><%= model_class.human_attribute_name(:psz_msg) %></th>
      <th><%= model_class.human_attribute_name(:psz_mobis) %></th>
      <th><%= model_class.human_attribute_name(:request_ip) %></th>
      <th><%= model_class.human_attribute_name(:channel_name) %></th>
      <th><%= model_class.human_attribute_name(:response_code) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <td><%= link_to message.id, message_path(message) %></td>
        <td><%= message.psz_sub_port %></td>
        <td><%= message.psz_msg %></td>
        <td><%= message.psz_mobis %></td>
        <td><%= message.request_ip %></td>
        <td><%= message.channel_name %></td>
        <td><%= message.response_code %></td>
        <td><%=l message.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_message_path(message), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      message_path(message),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_message_path,
            :class => 'btn btn-primary' %>
